<%- if controller_name != 'sessions' %>
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <h6 class="p-3 mb-2 bg-transparent text-dark">既に登録済みの方</h6>
    </div>
  </div>
  <div class="d-flex flex-column bd-highlight mb-3">
    <div class="mx-auto col-md-6">
      <%= link_to "こちら", new_session_path(resource_name) %>からログインしてください。
    </div>
  </div>
<% end %>

<%- if devise_mapping.registerable? && controller_name != 'registrations' %>
  <div class="row">
    <div class="col-md-8 offset-md-2">
      <h6 class="p-3 mb-2 bg-transparent text-dark">会員登録がお済でない方</h6>
    </div>
  </div>
  <div class="d-flex flex-column bd-highlight mb-3">
    <div class="mx-auto col-md-6">
      <%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。
    </div>
  </div>
<% end %>